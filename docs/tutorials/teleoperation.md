# Teleoperation

Start with the {doc}`../modules/task/teleoperation` task guide and the
{doc}`../modules/agent/mujoco` keyboard controls.

```bash
python example/unitree_g1/run_unitree_g1_teleop.py
```

The same task contract supports Cartesian targets, joint targets, gripper
commands, ROS 1, and ROS 2. Backend-specific setup is described under Agent.

# Safety-Aware Control

Run the Unitree G1 teleoperation example with a composed safety policy, select
an obstacle with `Space`, and move it using the arrow and `E`/`Q` keys. The
viewer displays collision volumes and active constraints.

See {doc}`../modules/policy/safety` for the implemented monitoring, filtering,
geometry, and tensor paths.

# Dynamics and Control

Use `DynamicsModelAgent` and `DynamicsTask` for controller and planner studies
that do not require a full physics scene. Robot configurations remain the
source of the dynamics equation and dimensions.

See {doc}`../architecture/dynamics_execution` and
{doc}`../modules/policy/control`.

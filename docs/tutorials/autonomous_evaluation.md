# Autonomous Evaluation

```bash
python example/unitree_g1/run_unitree_g1_benchmark.py --help
```

Benchmark pipelines combine reproducible goals, configurable environment
representations, completion metrics, and safety diagnostics. Start with a
short headless run before increasing duration or scene complexity.

# Configuration

Pipeline configurations select robot, kinematics, agent, task, policy, and
runtime parameters using nested classes. Specialized configurations inherit
shared defaults and override only the required fields.

Check the selected robot's dimensions and backend compatibility before
overriding policy or task parameters.

# Dynamics Task

`DynamicsTask` wraps a direct dynamics experiment. Its central parameters are
the initial state, maximum steps, and random seed. It pairs naturally with
`DynamicsModelAgent` and model-based control or planning policies.

# Cartesian Trajectory Task

`CartesianTrajectoryTrackingTask` accepts pose arrays or trajectory files for
the right arm, left arm, objects, and trajectory distributions. It supports
world or robot-base frames, full-horizon or waypoint execution, repeated
trajectories, grasp targets, object following, and debug rendering.

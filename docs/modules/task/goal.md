# Goal Task

`BaseGoalTask` supplies common episode, obstacle, arm-goal, base-goal,
randomization, and reset parameters. Completion can require any enabled goal
or all enabled goals. Independent flags control success, timeout, fall, and
goal resampling behavior.

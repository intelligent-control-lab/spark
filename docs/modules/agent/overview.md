# Agent Module Overview

An agent owns an execution backend, applies robot commands, advances or reads
the backend, and returns a common feedback dictionary. It does not define task
goals or select policies.

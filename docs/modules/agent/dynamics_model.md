# Dynamics-Model Agent

`DynamicsModelAgent` executes a `RobotDynamicsModel` without a physics scene.
It is suitable for deterministic controller, estimator, and planner studies,
including parameter updates, disturbances, and selectable numerical stepping.

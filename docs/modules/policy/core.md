# Core

The `core` package defines `Policy`, `BasePolicy`, components, configuration,
execution/reset contexts, typed ports, result objects, errors, and component
metadata. These contracts allow policies to expose inputs, outputs, execution
kind, purpose, and statefulness without embedding pipeline behavior.

# SparkEnvironment

`SparkEnvironment` is the single-agent implementation. It applies commands
through the agent, advances or reads the backend, updates the task from fresh
feedback, and returns the next state of the application. It does not select or
evaluate the policy.

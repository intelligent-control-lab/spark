# Pipeline Configuration

Configuration sections select the robot configuration, kinematics, agent,
task, policy or explicit policy composition, step/reset limits, timing,
visualization, and logging. Specialized pipelines inherit common defaults and
override only their workflow-specific choices.

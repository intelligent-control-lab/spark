# Performance

Profile environment stepping, policy evaluation, collision indexing, safety
optimization, rendering, and idle time separately. Match the control period to
the slowest required stage and reduce visualization frequency before changing
control semantics.

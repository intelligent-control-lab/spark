# Simulation and Deployment

Validate new robot, task, policy, and safety combinations in a deterministic
headless run, then an interactive simulator run, before hardware execution.
Confirm command limits, reset behavior, communication loss handling, and the
physical emergency stop independently.
